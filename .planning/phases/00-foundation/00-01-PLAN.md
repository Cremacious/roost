---
phase: 00-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/next.config.ts
  - apps/web/drizzle.config.ts
  - apps/web/tailwind.config.ts
  - apps/web/postcss.config.mjs
  - apps/web/jest.config.js
  - apps/web/jest.config.ts
  - apps/web/eslint.config.mjs
  - apps/web/playwright.config.ts
  - apps/web/next-env.d.ts
  - apps/web/.env.example
  - apps/web/public/
  - apps/web/src/
  - apps/web/e2e/
autonomous: true
requirements:
  - FOUN-01
  - FOUN-05
  - MIG-02
  - FOUN-02

must_haves:
  truths:
    - "Running `npm run dev` from the repo root starts the Next.js app on port 3000"
    - "Running `npm run build --workspace=apps/web` succeeds with zero TypeScript errors"
    - "All existing `@/` imports resolve correctly — no 'Module not found' errors"
    - "The rebuild/v2 git branch is the active branch and V1 master is untouched"
    - "vercel.json remains at the repo root with all 7 cron entries intact"
    - "The Vercel project Root Directory is set to apps/web/ in the Vercel dashboard (D-07 completed — one-time manual step required before first deploy of rebuild/v2 succeeds)"
  artifacts:
    - path: "package.json"
      provides: "npm workspace manifest"
      contains: "\"workspaces\""
    - path: "apps/web/package.json"
      provides: "web app package with all V1 dependencies"
      contains: "\"@roost/web\""
    - path: "apps/web/tsconfig.json"
      provides: "TypeScript config with @/ alias and @roost/* stubs"
      contains: "@roost/constants"
    - path: "apps/web/drizzle.config.ts"
      provides: "Drizzle config pointing to DEV_DATABASE_URL"
      contains: "DEV_DATABASE_URL"
    - path: "apps/web/src/"
      provides: "All V1 source code"
    - path: "vercel.json"
      provides: "Vercel cron config at repo root"
  key_links:
    - from: "package.json (root)"
      to: "apps/web/"
      via: "npm workspaces delegate scripts"
      pattern: "--workspace=apps/web"
    - from: "apps/web/tsconfig.json"
      to: "apps/web/src/"
      via: "@/ path alias"
      pattern: "\"@/\\*\": \\[\"./src/\\*\"\\]"
    - from: "apps/web/drizzle.config.ts"
      to: "DEV_DATABASE_URL"
      via: "process.env.DEV_DATABASE_URL"
      pattern: "DEV_DATABASE_URL"

user_setup:
  - service: vercel
    why: "Set the Vercel project Root Directory to apps/web/ in the Vercel dashboard"
    dashboard_config:
      - task: "Set Root Directory to apps/web/"
        location: "Vercel Dashboard -> Project -> Settings -> General -> Root Directory"
---

<objective>
Convert the flat repository into an npm workspace monorepo in a single atomic commit on the rebuild/v2 branch. All V1 source code moves to `apps/web/`, the repo root becomes the workspace manifest only, and all existing developer workflows (`npm run dev`, `npm run build`, etc.) continue to work from the root unchanged.

Purpose: FOUN-01 (monorepo structure) and FOUN-05 (rebuild/v2 branch). This is the prerequisite for all other Phase 0 work. Per D-01 and D-02, this must be a full move and a single atomic commit.

Output: A `rebuild/v2` branch where `npm run dev` from the root starts the web app, `npm run build --workspace=apps/web` passes, and vercel.json sits at the repo root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-foundation/00-CONTEXT.md
@.planning/phases/00-foundation/00-RESEARCH.md
@.planning/phases/00-foundation/00-PATTERNS.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md

<interfaces>
<!-- Current root package.json structure (executor must preserve all script names exactly) -->
<!-- Scripts in current package.json lines 5-21: dev, build, start, lint, db:generate, db:migrate, db:push, db:seed, db:studio, test, test:watch, test:coverage, test:e2e, test:e2e:ui, test:e2e:headed -->
<!-- All 60+ dependencies and devDependencies from current root package.json move to apps/web/package.json -->

Current drizzle.config.ts:
```typescript
import type { Config } from 'drizzle-kit'
import * as dotenv from 'dotenv'
dotenv.config({ path: '.env.local' })

export default {
  schema: './src/db/schema/index.ts',
  out: './drizzle',
  dialect: 'postgresql',
  dbCredentials: {
    url: process.env.DATABASE_URL!,
  },
} satisfies Config
```

Current tsconfig.json paths block:
```json
"paths": {
  "@/*": ["./src/*"]
}
```

Current vercel.json: stays at repo root (7 cron entries, do NOT move)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Create rebuild/v2 branch</name>
  <action>
Create and switch to the rebuild/v2 branch from master:

```bash
git checkout master
git pull origin master
git checkout -b rebuild/v2
```

This is the branch where all V2 work happens. V1 stays live on master.
  </action>
  <verify>
    <automated>git branch --show-current</automated>
  </verify>
  <done>Current branch is `rebuild/v2`. Master branch is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Create monorepo structure and move all V1 source</name>

  <read_first>
    - package.json (current root — ALL scripts and dependencies move to apps/web/)
    - tsconfig.json (current root — moves to apps/web/, paths updated)
    - drizzle.config.ts (current root — moves to apps/web/, two changes applied)
    - next.config.ts (current root — moves to apps/web/ verbatim)
    - vercel.json (stays at repo root — DO NOT MOVE)
    - .planning/phases/00-foundation/00-PATTERNS.md (exact target states for each file)
    - .planning/phases/00-foundation/00-RESEARCH.md (Pattern 1: root manifest, Pattern 2: @roost/* paths)
  </read_first>

  <action>
This task creates the complete monorepo structure in one atomic operation. Every file move and config change lands in a single git commit (D-02).

**Step 0: Branch guard (MUST be the first step)**

Before touching any files, verify the current branch is rebuild/v2. Running git add -A on master would corrupt V1.

```bash
git branch --show-current | grep -q 'rebuild/v2' || (echo 'ERROR: not on rebuild/v2 -- aborting' && exit 1)
```

If this exits non-zero, stop and switch to rebuild/v2 first (Task 1).

**Step 1: Create directory structure**

```bash
mkdir -p apps/web
mkdir -p apps/mobile
mkdir -p packages/api-types
mkdir -p packages/constants
mkdir -p packages/utils
```

**Step 2: Move ALL V1 source files to apps/web/**

Move these items from the repo root into `apps/web/`:
- `src/` directory (the entire source tree)
- `next.config.ts`
- `tailwind.config.ts`
- `postcss.config.mjs`
- `jest.config.js` (if exists) and/or `jest.config.ts` (if exists)
- `eslint.config.mjs`
- `playwright.config.ts`
- `next-env.d.ts`
- `.env.example`
- `public/` directory
- `e2e/` directory

Do NOT move:
- `vercel.json` — stays at repo root (per D-06)
- `package.json` — this file is being replaced (not moved)
- `tsconfig.json` — this file is being replaced (not moved)
- `drizzle.config.ts` — this file is being replaced (not moved)
- `.env.local` — stays at root for V1, new one created in apps/web/ (see Step 6)
- `package-lock.json` — will be regenerated

Use git mv for each item to preserve git history:
```bash
git mv src apps/web/src
git mv next.config.ts apps/web/next.config.ts
git mv tailwind.config.ts apps/web/tailwind.config.ts
git mv postcss.config.mjs apps/web/postcss.config.mjs
# move jest config (check which exists: jest.config.js or jest.config.ts or both)
git mv eslint.config.mjs apps/web/eslint.config.mjs
git mv playwright.config.ts apps/web/playwright.config.ts
git mv next-env.d.ts apps/web/next-env.d.ts
git mv .env.example apps/web/.env.example
git mv public apps/web/public
git mv e2e apps/web/e2e
```

**Step 3: Create the new root package.json (workspace manifest)**

Write `package.json` at repo root with this exact content:

```json
{
  "name": "roost-monorepo",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev":             "npm run dev --workspace=apps/web",
    "build":           "npm run build --workspace=apps/web",
    "start":           "npm run start --workspace=apps/web",
    "lint":            "npm run lint --workspace=apps/web",
    "db:push":         "npm run db:push --workspace=apps/web",
    "db:generate":     "npm run db:generate --workspace=apps/web",
    "db:migrate":      "npm run db:migrate --workspace=apps/web",
    "db:seed":         "npm run db:seed --workspace=apps/web",
    "db:studio":       "npm run db:studio --workspace=apps/web",
    "test":            "npm run test --workspace=apps/web",
    "test:watch":      "npm run test:watch --workspace=apps/web",
    "test:coverage":   "npm run test:coverage --workspace=apps/web",
    "test:e2e":        "npm run test:e2e --workspace=apps/web",
    "test:e2e:ui":     "npm run test:e2e:ui --workspace=apps/web",
    "test:e2e:headed": "npm run test:e2e:headed --workspace=apps/web"
  }
}
```

**Step 4: Create apps/web/package.json**

Copy the content from the original root `package.json` (all dependencies and devDependencies intact) and change only the `name` field to `"@roost/web"`. The `scripts` section is preserved as-is (they run correctly from the `apps/web/` working directory). The `db:seed` script path `src/db/seed.ts` stays the same because it is relative to `apps/web/`.

**Step 5: Create apps/web/tsconfig.json**

Copy the current `tsconfig.json` verbatim, then add `@roost/*` entries to the `paths` block. The `@/*` entry `"./src/*"` is unchanged (correct because the tsconfig now lives in `apps/web/` and `src/` is `apps/web/src/`):

```json
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": {
      "@/*": ["./src/*"],
      "@roost/constants": ["../../packages/constants/index.ts"],
      "@roost/api-types": ["../../packages/api-types/index.ts"],
      "@roost/utils": ["../../packages/utils/index.ts"]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    "**/*.mts",
    ".next/dev/types/**/*.ts"
  ],
  "exclude": ["node_modules"]
}
```

**Step 6: Create apps/web/drizzle.config.ts**

Create with two changes from the original: `out` changes from `'./drizzle'` to `'./migrations'`, and `DATABASE_URL` changes to `DEV_DATABASE_URL`:

```typescript
// V2: uses DEV_DATABASE_URL — never DATABASE_URL (production safety)
import type { Config } from 'drizzle-kit'
import * as dotenv from 'dotenv'
dotenv.config({ path: '.env.local' })

export default {
  schema: './src/db/schema/index.ts',
  out: './migrations',
  dialect: 'postgresql',
  dbCredentials: {
    url: process.env.DEV_DATABASE_URL!,
  },
} satisfies Config
```

**Step 7: Remove the old root config files**

The original `tsconfig.json` and `drizzle.config.ts` at the repo root are now superseded. Delete them:
```bash
git rm tsconfig.json
git rm drizzle.config.ts
```

The original root `package.json` was overwritten in Step 3, so no explicit deletion needed.

**Step 8: Create stub packages**

For each of `packages/api-types`, `packages/constants`, `packages/utils`, create two files:

`packages/constants/package.json`:
```json
{
  "name": "@roost/constants",
  "version": "0.0.1",
  "private": true,
  "main": "index.ts",
  "types": "index.ts"
}
```

`packages/constants/index.ts`:
```typescript
// Stub. Real exports extracted from apps/web/src/lib/constants/ in Phase 2.
export {};
```

`packages/api-types/package.json`:
```json
{
  "name": "@roost/api-types",
  "version": "0.0.1",
  "private": true,
  "main": "index.ts",
  "types": "index.ts"
}
```

`packages/api-types/index.ts`:
```typescript
// Stub. Real exports extracted from API route types in Phase 2.
export {};
```

`packages/utils/package.json`:
```json
{
  "name": "@roost/utils",
  "version": "0.0.1",
  "private": true,
  "main": "index.ts",
  "types": "index.ts"
}
```

`packages/utils/index.ts`:
```typescript
// Stub. Real exports extracted from apps/web/src/lib/utils/ in Phase 2.
export {};
```

**Step 9: Install workspace dependencies**

From the repo root, run:
```bash
npm install
```

This creates the workspace symlinks under `node_modules/` and regenerates `package-lock.json`.

**Step 10: Commit atomically**

Stage and commit all changes in a single commit (D-02):
```bash
git add -A
git commit -m "feat(foundation): establish npm workspace monorepo structure (D-01, D-02)"
```
  </action>

  <verify>
    <automated>
# Branch guard: confirm we are on rebuild/v2
git branch --show-current | grep -q 'rebuild/v2' && echo "PASS: on rebuild/v2" || echo "FAIL: wrong branch"

# Verify workspace manifest
grep -c "workspaces" package.json

# Verify apps/web has source
ls apps/web/src/app/layout.tsx

# Verify drizzle config uses DEV_DATABASE_URL and not DATABASE_URL
grep "DEV_DATABASE_URL" apps/web/drizzle.config.ts
grep -v "^//" apps/web/drizzle.config.ts | grep -c "DATABASE_URL!" && echo "PASS: only DEV_DATABASE_URL"

# Verify @roost/* paths in tsconfig
grep "@roost/constants" apps/web/tsconfig.json

# Verify vercel.json still at root with cron entries
grep -c "cron" vercel.json

# Verify stub packages exist
ls packages/constants/index.ts packages/api-types/index.ts packages/utils/index.ts

# Verify delegate scripts in root package.json
grep "workspace=apps/web" package.json | wc -l
    </automated>
  </verify>

  <done>
- `git branch --show-current` outputs `rebuild/v2`
- `package.json` at root contains `"workspaces": ["apps/*", "packages/*"]` and all delegate scripts
- `apps/web/src/` exists with the full V1 source tree
- `apps/web/package.json` has `"name": "@roost/web"` and all original dependencies
- `apps/web/tsconfig.json` contains `"@roost/constants"` in paths
- `apps/web/drizzle.config.ts` references `DEV_DATABASE_URL` (never `DATABASE_URL`)
- `vercel.json` is at the repo root with all 7 cron entries
- All three stub packages exist with `package.json` and empty `index.ts`
- A single atomic commit lands all changes on rebuild/v2 branch
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify build succeeds post-restructure</name>

  <read_first>
    - apps/web/package.json (confirm all deps present)
    - apps/web/tsconfig.json (confirm paths)
    - apps/web/next.config.ts (confirm relative imports still valid)
  </read_first>

  <action>
Run the Next.js build from the workspace root to verify zero broken imports or TypeScript errors. This is the acceptance gate for the atomic commit.

```bash
# Run build via root delegate script (exercises the full workspace delegation path)
npm run build
```

If the build fails with `Module not found: Can't resolve '@/...'` — the tsconfig `@/*` path alias is not resolving. Check that `apps/web/tsconfig.json` has `"@/*": ["./src/*"]` (relative path, not absolute).

If the build fails with `Cannot find module '../../packages/constants/index.ts'` — the stub package path is wrong. Check the relative path from `apps/web/` to `packages/` (should be `../../packages/`).

If the build fails with `Cannot find module './src/lib/security/csp'` in `next.config.ts` — the relative import is broken. The file moved alongside `src/`, so this should not occur.

Do NOT skip or ignore TypeScript errors. All errors must be resolved before the commit stands.

After a successful build, verify the dev server starts on port 3000:
```bash
(npm run dev &) && sleep 8 && curl -sf http://localhost:3000 -o /dev/null -w '%{http_code}' | grep -q '200\|307\|308' && kill %1 && echo PASS || (kill %1 2>/dev/null; echo FAIL)
```
Expected: PASS (HTTP 200 or 307 redirect to login).
  </action>

  <verify>
    <automated>
npm run build --workspace=apps/web 2>&1 | tail -5
(npm run dev &) && sleep 8 && curl -sf http://localhost:3000 -o /dev/null -w '%{http_code}' | grep -q '200\|307\|308' && kill %1 && echo "PORT_3000: PASS" || (kill %1 2>/dev/null; echo "PORT_3000: FAIL")
    </automated>
  </verify>

  <done>`npm run build` completes with exit code 0 and no TypeScript compilation errors. The output includes "Route (app)" summary table. `npm run dev` starts on port 3000 and returns HTTP 200 or 307.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo root .env.local | Must never contain DATABASE_URL (production credential) in V2 |
| apps/web/.env.local | V2 env file — DEV_DATABASE_URL only, no production connection string |
| git branch | rebuild/v2 must never be deployed to the Vercel production slot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | Tampering | apps/web/drizzle.config.ts | mitigate | Config reads DEV_DATABASE_URL only; never DATABASE_URL. Comment in file: "V2: uses DEV_DATABASE_URL — never DATABASE_URL". Any accidental addition of DATABASE_URL to apps/web/.env.local would target production. Guard: drizzle.config.ts uses `process.env.DEV_DATABASE_URL!` with no fallback. |
| T-00-02 | Spoofing | rebuild/v2 branch | mitigate | Vercel project is pinned to master branch. rebuild/v2 deploys only to preview URLs. No production traffic until Phase 4 cutover. |
| T-00-03 | Information Disclosure | package-lock.json | accept | Lock file committed as normal. Contains only package metadata, no secrets. Low risk. |
| T-00-04 | Tampering | vercel.json (repo root) | accept | Assumption A6 (Vercel reads root vercel.json when Root Directory = apps/web/) must be verified against Vercel docs before D-07 is executed. If false, crons break silently. Risk accepted here; D-07 is a manual step in user_setup. |
</threat_model>

<verification>
From the repo root on the rebuild/v2 branch:

1. `git branch --show-current` → `rebuild/v2`
2. `cat package.json | grep workspaces` → shows `"apps/*"` and `"packages/*"`
3. `npm run build` → exits 0, no TypeScript errors
4. `grep "DEV_DATABASE_URL" apps/web/drizzle.config.ts` → matches
5. `grep -v "DATABASE_URL" apps/web/drizzle.config.ts | grep -c "DATABASE_URL"` → 0 (no unguarded production URL reference)
6. `grep -c "cron" vercel.json` → 7 (all cron entries intact)
7. `ls packages/constants/index.ts packages/api-types/index.ts packages/utils/index.ts` → all three exist
8. `grep "@roost/constants" apps/web/tsconfig.json` → matches
</verification>

<success_criteria>
- rebuild/v2 branch exists and is checked out
- `npm run build` from repo root succeeds with exit code 0
- `apps/web/src/` contains the full V1 source tree
- `apps/web/drizzle.config.ts` references only `DEV_DATABASE_URL`, never `DATABASE_URL`
- `vercel.json` is at the repo root with 7 cron entries
- Three stub packages exist under `packages/`
- Single atomic commit contains all restructure changes
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation/00-01-SUMMARY.md`
</output>
